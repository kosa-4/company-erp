# Purchase ERP 프론트엔드 구조 가이드


## 프로젝트 개요

### 기술 스택
- **백엔드**: Spring Boot 3.5.9, Java 17, Gradle
- **프론트엔드**: Next.js 16.1.1, React 19.2.3, TypeScript 5, Tailwind CSS 4
- **IDE**: STS (Spring Tool Suite) / IntelliJ IDEA


## 프론트엔드 구조 (Next.js)

### 📁 디렉토리 구조

```
frontend/
├── src/
│   ├── app/                                  # Next.js App Router
│   │   ├── layout.tsx                        # 루트 레이아웃
│   │   ├── globals.css                       # 전역 스타일
│   │   │
│   │   └── (main)/                           # 메인 라우트 그룹
│   │       ├── layout.tsx                    # 메인 레이아웃 (사이드바, 헤더)
│   │       ├── page.tsx                      # 대시보드 (/)
│   │       │
│   │       ├── mypage/                       # 마이페이지
│   │       │   ├── profile/
│   │       │   │   └── page.tsx              # /mypage/profile
│   │       │   └── notice/
│   │       │       └── page.tsx              # /mypage/notice
│   │       │
│   │       ├── master/                       # 기준정보
│   │       │   ├── item/
│   │       │   │   └── page.tsx              # /master/item
│   │       │   ├── vendor/
│   │       │   │   └── page.tsx              # /master/vendor
│   │       │   └── vendor-user/
│   │       │       └── page.tsx              # /master/vendor-user
│   │       │
│   │       ├── purchase/                     # 구매관리
│   │       │   ├── request/
│   │       │   │   └── page.tsx              # /purchase/request
│   │       │   └── request-list/
│   │       │       └── page.tsx              # /purchase/request-list
│   │       │
│   │       ├── rfq/                          # 견적관리
│   │       │   ├── pending/
│   │       │   │   └── page.tsx              # /rfq/pending
│   │       │   ├── progress/
│   │       │   │   └── page.tsx              # /rfq/progress
│   │       │   ├── selection/
│   │       │   │   └── page.tsx              # /rfq/selection
│   │       │   └── result/
│   │       │       └── page.tsx              # /rfq/result
│   │       │
│   │       ├── order/                        # 발주관리
│   │       │   ├── pending/
│   │       │   │   └── page.tsx              # /order/pending
│   │       │   └── progress/
│   │       │       └── page.tsx              # /order/progress
│   │       │
│   │       └── inventory/                   # 재고관리
│   │           ├── receiving-target/
│   │           │   └── page.tsx              # /inventory/receiving-target
│   │           └── receiving-list/
│   │               └── page.tsx              # /inventory/receiving-list
│   │
│   ├── components/                           # React 컴포넌트
│   │   ├── layout/                           # 레이아웃 컴포넌트
│   │   │   ├── MainLayout.tsx                # 메인 레이아웃
│   │   │   ├── Sidebar.tsx                   # 사이드바
│   │   │   ├── Header.tsx                    # 헤더
│   │   │   ├── Breadcrumb.tsx                # 브레드크럼
│   │   │   └── index.ts                      # export 모음
│   │   │
│   │   └── ui/                               # UI 컴포넌트
│   │       ├── Button.tsx                    # 버튼
│   │       ├── Input.tsx                     # 입력 필드
│   │       ├── DataGrid.tsx                 # 데이터 그리드
│   │       ├── Modal.tsx                     # 모달
│   │       ├── Badge.tsx                     # 배지
│   │       ├── Card.tsx                      # 카드
│   │       ├── Select.tsx                    # 셀렉트
│   │       ├── DatePicker.tsx               # 날짜 선택
│   │       ├── SearchPanel.tsx               # 검색 패널
│   │       ├── PageHeader.tsx                # 페이지 헤더
│   │       ├── Textarea.tsx                  # 텍스트 영역
│   │       └── index.ts                      # export 모음
│   │
│   ├── constants/                            # 상수 정의
│   │   └── navigation.ts                     # 네비게이션 메뉴
│   │
│   └── types/                                # TypeScript 타입
│       └── index.ts                          # 공통 타입 정의
│
├── public/                                   # 정적 파일
│   ├── favicon.ico
│   └── ...
│
├── package.json                              # 의존성 관리
├── tsconfig.json                             # TypeScript 설정
├── next.config.ts                            # Next.js 설정
└── postcss.config.mjs                        # PostCSS 설정
```

### 📝 주요 디렉토리 설명

#### 1. `app/` 디렉토리 (Next.js App Router)
- **역할**: 페이지 라우팅 및 레이아웃
- **규칙**:
  - `page.tsx`: 페이지 컴포넌트
  - `layout.tsx`: 레이아웃 컴포넌트
  - `(main)`: 라우트 그룹 (URL에 포함되지 않음)
  - `'use client'`: 클라이언트 컴포넌트 선언

#### 2. `components/` 디렉토리
- **구조**:
  - `layout/`: 레이아웃 관련 컴포넌트
  - `ui/`: 재사용 가능한 UI 컴포넌트
- **규칙**:
  - 각 컴포넌트는 독립 파일로 작성
  - `index.ts`로 export 통합
  - Props는 TypeScript interface로 정의

#### 3. `types/` 디렉토리
- **역할**: TypeScript 타입 정의
- **주요 타입**:
  - `User`, `Item`, `Vendor`
  - `PurchaseRequestHeader`, `PurchaseRequestDetail`
  - `RfqHeader`, `RfqDetail`
  - `PoHeader`, `PoDetail`
  - `GrHeader`, `GrDetail`
  - `ApiResponse<T>`, `Pagination`

#### 4. `constants/` 디렉토리
- **역할**: 상수 및 설정값 정의
- **주요 파일**:
  - `navigation.ts`: 사이드바 메뉴 구조

### 🎨 스타일링 (Tailwind CSS)

#### 전역 스타일 (`globals.css`)
- CSS 변수 정의 (`:root`)
- 테마 색상 (Primary: Teal, Secondary: Stone)
- 공통 유틸리티 클래스
- 애니메이션 정의

#### 사용 예시
```tsx
// Tailwind 클래스 사용
<div className="bg-white rounded-xl border border-stone-200 p-5">
  <h1 className="text-lg font-semibold text-stone-900">제목</h1>
</div>
```

---

## 개발 환경 설정

### STS (Spring Tool Suite) 설정

#### 1. 프로젝트 Import
```
File → Import → Gradle → Existing Gradle Project
→ 프로젝트 루트 디렉토리 선택
```

#### 2. Java 버전 확인
- **필요 버전**: Java 17
- **확인 방법**: `Window → Preferences → Java → Installed JREs`

#### 3. Gradle 설정
- **Gradle Wrapper 사용**: 프로젝트에 포함된 `gradlew` 사용
- **빌드**: `Run → Run Configurations → Gradle Task`

#### 4. 백엔드 실행
```bash
# 터미널에서
./gradlew bootRun

# 또는 STS에서
Run → Run As → Spring Boot App
```

### IntelliJ IDEA 설정

#### 1. 프로젝트 Open
```
File → Open → 프로젝트 루트 디렉토리 선택
→ "Open as Project" 선택
```

#### 2. Gradle 설정
- **Gradle JVM**: Java 17 선택
- **Build Tool**: Gradle 선택
- **Auto-import**: 활성화

#### 4. 프론트엔드 실행
```
1. Terminal 탭 열기 (Alt + F12)
2. cd frontend
3. npm install (최초 1회)
4. npm run dev
```


#### 프론트엔드 (환경 변수 필요 시)
```bash
# .env.local 파일 생성
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
```

---




### 프론트엔드 (TypeScript/React)

#### 컴포넌트 네이밍
- **컴포넌트 파일**: PascalCase (`UserProfile.tsx`)
- **함수 컴포넌트**: PascalCase (`const UserProfile: React.FC = ...`)
- **Props 인터페이스**: `ComponentNameProps` (`UserProfileProps`)

#### 컴포넌트 구조
```tsx
'use client';

import React from 'react';

interface UserProfileProps {
  userId: string;
  userName: string;
}

const UserProfile: React.FC<UserProfileProps> = ({ userId, userName }) => {
  return (
    <div className="p-4">
      <h2>{userName}</h2>
    </div>
  );
};

export default UserProfile;
```

#### 타입 정의
```typescript
// types/index.ts
export interface User {
  id: string;
  userId: string;
  userName: string;
  email: string;
}

export interface ApiResponse<T> {
  success: boolean;
  data: T;
  message?: string;
}
```

---

## 주요 기능 모듈

### 1. 마이페이지 (`/mypage`)
- **내 정보 수정** (`/mypage/profile`)
- **공지사항** (`/mypage/notice`)

### 2. 기준정보 (`/master`)
- **품목 현황** (`/master/item`)
- **협력업체 현황** (`/master/vendor`)
- **협력업체 사용자 관리** (`/master/vendor-user`)

### 3. 구매관리 (`/purchase`)
- **구매요청** (`/purchase/request`)
- **구매요청 현황** (`/purchase/request-list`)

### 4. 견적관리 (`/rfq`)
- **견적대기목록** (`/rfq/pending`)
- **견적진행현황** (`/rfq/progress`)
- **협력업체 선정** (`/rfq/selection`)
- **협력업체 선정결과** (`/rfq/result`)

### 5. 발주관리 (`/order`)
- **발주대기목록** (`/order/pending`)
- **발주진행현황** (`/order/progress`)

### 6. 재고관리 (`/inventory`)
- **입고대상조회** (`/inventory/receiving-target`)
- **입고현황** (`/inventory/receiving-list`)

---

## API 엔드포인트 구조 (예정)

### 공통 규칙
- **Base URL**: `/api/v1`
- **응답 형식**: `ApiResponse<T>`
- **인증**: JWT 토큰 (추가 예정)

### 예시 엔드포인트
```
GET    /api/v1/users/{id}              # 사용자 조회
POST   /api/v1/users                   # 사용자 생성
PUT    /api/v1/users/{id}              # 사용자 수정
DELETE /api/v1/users/{id}              # 사용자 삭제

GET    /api/v1/items                   # 품목 목록 조회
POST   /api/v1/items                   # 품목 생성
GET    /api/v1/items/{code}            # 품목 상세 조회

GET    /api/v1/purchase-requests       # 구매요청 목록
POST   /api/v1/purchase-requests      # 구매요청 생성
GET    /api/v1/purchase-requests/{no}  # 구매요청 상세
```

---

## 참고 자료

### 공식 문서
- [Spring Boot Documentation](https://spring.io/projects/spring-boot)
- [Next.js Documentation](https://nextjs.org/docs)
- [React Documentation](https://react.dev)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)


