<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.erp.po.mapper.DocNoMapper">

    <select id="getDocNo" parameterType="map" statementType="CALLABLE">
        {CALL getDocNum(#{docType, mode=IN}, #{docNo, mode=OUT, jdbcType=VARCHAR})}
    </select>

</mapper>